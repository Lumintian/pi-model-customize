# AGENTS.md

本文件适用于 `pi-model-customize` 整个项目。

## 项目定位

这是 pi coding agent 的 TypeScript 扩展，按模型定制思考档位、默认思考强度、上下文窗口与最大输出 token 数。通过全局及受信任项目的 JSON/JSONC 配置驱动，直接修补内存中的模型对象，不改写用户的 `settings.json` 或 `models.json`。

开始修改前阅读 `README.md`；配置语义见 `docs/configuration.md`，生命周期与撤销机制见 `docs/internals.md`。涉及 pi API 或事件行为时，核对当前依赖版本的官方文档、类型及扩展示例，不凭旧版本经验猜测接口。

## 目录与职责

- `extensions/index.ts`：扩展入口、会话事件、配置重载、默认思考档位策略，以及 `/mc`、`/model-customize` 诊断命令。
- `src/config.ts`：JSONC 处理、配置校验、全局/项目配置读取与合并。
- `src/rules.ts`：规则类型、模式匹配、定制解析与应用、CLI 思考参数识别。
- `src/customizer.ts`：模型属性快照、重复应用与所有权友好的撤销。
- `tests/config.test.ts`：配置解析、校验与加载测试。
- `tests/rules.test.ts`：匹配、思考能力、参数修补与撤销等测试。
- `tests/extension.test.ts`：扩展事件、诊断与会话行为测试。
- `examples/`：用户配置示例；`docs/`：详细配置与运行机制文档。

保持边界：文件读取与校验放在配置层，规则计算放在规则层，事件编排放在入口，快照管理放在 customizer。不要把可独立测试的逻辑堆进入口。

## 必须保持的行为

除非任务明确要求改变，下列行为属于兼容性约束：

1. **信任边界**：只有 `ctx.isProjectTrusted()` 为真时才读取项目配置。全局目录通过 `getAgentDir()` 获取，项目配置基于 `ctx.cwd` 与 `CONFIG_DIR_NAME`，不要硬编码机器路径。
2. **容错隔离**：缺失配置视为空规则；某一层配置无效时报告警告并忽略该文件，不影响另一层有效配置。
3. **规则优先级**：项目模式列表在全局模式之前，只采用首个匹配模式；随后按字段叠加纯模型 ID 覆盖，再叠加 `provider/模型ID` 覆盖。同一精确键的项目字段覆盖全局字段。
4. **模式语义**：字符串通配符忽略大小写，精确键区分大小写。保留模式预编译与正则 `lastIndex` 重置，避免带 `g`/`y` 标志时出现交替命中。
5. **能力边界**：白名单只能收窄原生思考能力；显式映射与白名单的优先关系遵循配置文档。非推理模型跳过思考配置，但仍可调整 token 限制。token 限制必须是正安全整数。
6. **会话意图**：启动及新建会话尊重 CLI 显式思考参数；恢复、派生和重载保留当前档位并按能力钳制。主动切换模型应用目标默认档位；`model_select` 的 `restore` 来源不重新设置档位。
7. **可逆修补**：记录原始属性值及属性是否自身存在。只撤销仍等于本插件所应用值的属性，不覆盖其他扩展的后续修改。重复应用前先撤销旧修补；重载和关闭不能遗留陈旧限制。
8. **诊断兼容**：保留两个诊断命令，并兼容有 UI 的通知路径与无 UI 的控制台输出路径。

## 修改与交付

- 沿用现有代码风格：双引号、分号、两空格缩进、明确的类型和小函数。无关重构、格式化与新增依赖不混入功能修复。
- 修复缺陷时添加回归测试；配置行为变更覆盖解析、合并和优先级，生命周期变更覆盖对应事件，内存修补变更覆盖撤销及其他扩展修改属性的情况。
- 涉及 `/reload`、选模或交互诊断的变更，如有可用 pi 环境，应补充相应手工验证；不要为了测试而擅自安装扩展或改写用户全局配置。
- 用户可见行为或配置字段变更时，同步更新 `README.md`、相关 `docs/` 文档及 `examples/`，避免多处说明矛盾。用户文档优先使用中文。
- 提交前检查改动范围；不覆盖用户已有修改，不提交凭据、本机绝对路径、临时输出或 `node_modules/`。交付时简述修改文件、行为影响与验证结果。
